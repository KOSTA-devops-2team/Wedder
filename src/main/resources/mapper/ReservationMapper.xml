<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.wedder.dao.ReservationMapper">
    <insert id="insertReservationInfo" parameterType="kr.co.wedder.mypage.domain.ReservationDto">
        INSERT INTO reservation (company_id, customer_id, reservation_date, reservation_time, visit_reservation, hope_date)
        VALUES (#{companyId}, #{customerId}, #{reservationDate}, #{reservationTime}, #{visitReservation}, #{hopeDate})
    </insert>
</mapper>
